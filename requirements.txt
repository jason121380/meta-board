fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
asyncpg==0.29.0
# PDF export for AI 幕僚 reports — renders HTML/CSS server-side
# via Pango, with Noto Sans CJK installed at the OS layer (see
# Dockerfile). markdown-it-py converts the agent's markdown
# response into the HTML weasyprint consumes.
weasyprint==62.3
markdown-it-py==3.0.0
